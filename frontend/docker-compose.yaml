version: '3'
services:
  nginx:
    image: nginx
    ports:
      - "8080:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - /home/lepick/certs:/etc/letsencrypt/
      - ./dist:/var/www/bookinghotelservice.ru/
